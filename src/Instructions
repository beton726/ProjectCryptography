При вызове метода types в него передаётся путь (pathFiles) к папке с файлами, которые надо подписать.
На данной версии в папке могут быть только без ЭЦП(электронная цифровая подпись) файлы.
Файлы должны иметь любой тип.

